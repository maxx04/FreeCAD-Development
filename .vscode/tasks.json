{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "CMake: build FCProjectCpp",
            "type": "shell",
            "command": "cmake -S FCProjectCpp -B build && cmake --build build --target FCProjectCpp",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": false
            },
            "presentation": {
                "reveal": "always",
                "panel": "new"
            },
            "problemMatcher": []
        }
    ]

}