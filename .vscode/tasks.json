{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run in FreeCAD",
            "type": "shell",
            "command": "echo \"exec(open('${file}').read())\" | nc -q 0 localhost 9999",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "presentation": {
                "reveal": "always",
                "panel": "new"
            },
            "problemMatcher": []
        }
    ]
}
